{
  "name": "neg-reviews",
  "version": "1.0.0",
  "private": true,
  "description": "Who posted it — replies in Slack with the Get Licensed trainee booking ID for incoming review alerts",
  "main": "dist/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit -p tsconfig.all.json",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "replay": "tsx scripts/replay.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "@modelcontextprotocol/sdk": "^1.0.4",
    "@slack/web-api": "^7.8.0",
    "express": "^4.21.2",
    "mysql2": "^3.11.5"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
